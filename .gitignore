.env
__pycache__/
*.pyc

This refactored code integrates the new endpoint `/duffel-flights-list-offers` into the existing backend application. The endpoint is configured to handle POST requests and interact with the Duffel API using the provided API key. The code also includes necessary configurations for CORS and environment variable management. The application is set to run on port 5000.